# Git
Reimplementation of Git in Python

# Set Up
Run ```chmod +x [file]``` to make the files executable.<br><br>
Ex. ```chmod +x git-add```

# Features added
```git-add```<br><br>
```git-branch```<br><br>
```git-checkout```<br><br>
```git-commit```<br><br>
```git-init```<br><br>
```git-log```<br><br>
```git-rm```<br><br>
```git-show```<br><br>
```git-status```<br><br>
